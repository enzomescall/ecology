# Reminders
Always write clean, concise, readable code. Do not focus on fallbacks or backwards compatability. Small files and small functions. Reduce lines don't add.

# GameBoard
- The squares should dynamically generate around where we put the tiles, to a max of 5 tiles across and 4 tall. 
- The first tile doesn't have to be the center of the board, e.g. after the first tile we can go 4 more tiles to the left

# Home
- The navbar at the top of the page looks funky, the items on it need to be restructured

# EmailSent
- Implement logging in functionality, send a OTC to the email and require it to log in. Remember users in our db by their emails

# Game logic implementation
Go and look at docs/scoring-spec.md and docs/turn-spec.md for how the game logic works 